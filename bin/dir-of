#!/bin/sh
D=$1
if [ "$1" = "." ] || echo "$1" | grep "^\./" > /dev/null
then
	$0 "$PWD${1#?}"
elif echo "$1" | grep "^\/" > /dev/null
then
	echo "$1" | sed -e 'ss/[^/]*$ss' -e 'ss^$s/s'
else
	$0 "$PWD/$1"
fi
